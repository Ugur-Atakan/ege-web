import '../globals.css'

export const metadata = {
  title: 'Registate',
  description: '',
}

export default function RootLayout({ children }) {
  return (
    <html>
      <body>{children}</body>
    </html>
  )
}
