'use client'

import React from 'react'
import Settings from './components/Settings'

const Page = async (params) => {
  return(
    <React.Fragment>
        <Settings />
    </React.Fragment>
  )
}

export default Page